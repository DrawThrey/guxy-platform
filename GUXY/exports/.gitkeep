# 导出资源文件夹

此文件夹用于存放 GUXY 导出的所有资源。

## 文件夹结构

```
exports/
├── 2025-02-09_14-30-00/
│   ├── architecture.json      # 架构数据（JSON格式）
│   └── architecture.md        # 架构文档（Markdown格式）
├── 2025-02-09_15-45-20/
│   ├── architecture.json
│   └── architecture.md
└── ...
```

## 文件夹命名规则

每个导出操作会创建一个带时间戳的子文件夹：
- 格式：`YYYY-MM-DD_HH-mm-ss`
- 示例：`2025-02-09_14-30-00`

## 导出文件类型

### 架构导出
- `architecture.json` - 架构数据的JSON格式，包含集合、卡片、连线等完整信息
- `architecture.md` - 架构文档的Markdown格式，易于阅读和理解

### 分析导出
- `analysis.json` - 分析结果的JSON格式
- `analysis.md` - 分析文档的Markdown格式

## 使用说明

1. 导出操作会自动创建时间戳文件夹
2. 每个文件夹包含该次导出的所有相关文件
3. JSON 文件适合程序读取和处理
4. Markdown 文件适合人类阅读和文档化

## 注意事项

- 此文件夹内容可不纳入版本库（通过 svn:ignore 或本地忽略配置）
- 请定期清理旧的导出文件以节省空间
- 建议将重要的导出文件备份到其他位置

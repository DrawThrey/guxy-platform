779|【扩写须包含以下四部分，内容必须全部来自对当前界面的推断，不得套用模板】
780|
781|① 跳转逻辑：根据连线与界面层级，写该界面从哪里进入、可跳转到哪、如何返回。
782|
783|② 功能与配置：该界面实际包含的功能、涉及的道具/信息配置（若没有则写"无特殊配置"或略过）。
784|
785|③ 布局：详细描述该界面的每个分区，必须包含：
786|   - 分区位置：明确每个分区在界面上的位置（如顶部、中部、底部、左侧、右侧、中央等）
787|   - 分区包含的信息：列出该分区显示的具体信息内容（如角色属性、物品列表、任务描述等）
788|   - 分区内的组件类型：描述该分区包含的UI组件（如按钮、列表、文本、图标、输入框、进度条、标签页等）
789|   要求：对每个分区都要按上述三点详细描述，不要只列出分区名称。无道具/背包则不要写道具栏或背包；有再写并可按需注明尺寸。
790|
791|④ 状态：仅针对③中写到的每个区域，写该区域实际存在的状态（如列表有选中/未选、按钮有默认/悬停/禁用、内容区有加载中/空状态等）。不要给每个区域都套"选中、置灰、锁定"三件套。
792|
793|【当前界面信息】
